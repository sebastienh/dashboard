package dashboard.command;

/**
 * The global abstract Command super object. 
 * 
 * There is case we need global support in all Commands. 
 * 
 * @author sebastienhamel
 *
 */
public abstract class AbstractCommand implements Command {
	
}
